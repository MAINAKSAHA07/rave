{
  "name": "rave-ticketing-platform",
  "version": "1.0.0",
  "description": "Modern event ticketing platform for India with multi-currency support",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "backoffice"
  ],
  "scripts": {
    "dev": "docker-compose up -d pocketbase && sleep 3 && concurrently \"npm run dev:frontend\" \"npm run dev:backoffice\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backoffice": "cd backoffice && npm run dev",
    "dev:pocketbase": "docker-compose up -d pocketbase",
    "pb:start": "docker-compose up -d pocketbase",
    "pb:stop": "docker-compose down pocketbase",
    "pb:logs": "docker-compose logs -f pocketbase",
    "pb:backup": "docker-compose run --rm backup",
    "build": "npm run build:frontend && npm run build:backoffice",
    "build:frontend": "cd frontend && npm run build",
    "build:backoffice": "cd backoffice && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}